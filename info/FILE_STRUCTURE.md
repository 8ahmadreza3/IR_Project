# 📂 فهرست کامل پروژه

## فایل‌های اصلی

### ماژول‌های Python (7 فایل)

#### 1. `preprocessing.py` (4.4 KB)

**مسئولیت**: پیش‌پردازش اسناد و ساخت فرهنگ‌لغت

```python
PreProcessor class:
  - lowercase()              # تبدیل به حروف کوچک
  - remove_punctuation()     # حذف علائم
  - tokenize()               # تقسیم به واژه‌ها
  - stem()                   # ریشه‌یابی (اختیاری)
  - preprocess_text()        # تمام مراحل
  - load_documents()         # بارگذاری اسناد
  - build_dictionary()       # ساخت فرهنگ‌لغت
  - save_dictionary()        # ذخیره فرهنگ‌لغت
  - get_statistics()         # آمار پردازش
```

#### 2. `inverted_index.py` (3.3 KB)

**مسئولیت**: ساخت و مدیریت نمایه معکوس

```python
InvertedIndex class:
  - build()                  # ساخت نمایه
  - search()                 # جستجوی AND
  - search_or()              # جستجوی OR
  - get_posting_list()       # دریافت فهرست
  - save_to_file()           # ذخیره
  - load_from_file()         # بارگذاری
```

#### 3. `compression.py` (4.4 KB)

**مسئولیت**: فشردگی‌سازی نمایه با Gap Encoding

```python
IndexCompression class:
  - gap_encode()             # فشرده‌سازی
  - gap_decode()             # استخراج
  - compress_index()         # فشردگی کل نمایه
  - decompress_index()       # استخراج کل نمایه
  - get_compression_ratio()  # نسبت فشردگی
  - save_compressed_index()  # ذخیره
  - load_compressed_index()  # بارگذاری
```

#### 4. `vector_space_model.py` (6.0 KB)

**مسئولیت**: مدل فضای برداری، TF-IDF و رتبه‌بندی

```python
VectorSpaceModel class:
  - calculate_tf()           # محاسبه TF
  - calculate_idf()          # محاسبه IDF
  - calculate_tfidf()        # محاسبه TF-IDF
  - get_query_vector()       # بردار پرسوجو
  - cosine_similarity()      # شباهت
  - rank_documents()         # رتبه‌بندی
  - save_tfidf()             # ذخیره TF-IDF
```

#### 5. `query_expansion.py` (4.6 KB)

**مسئولیت**: گسترش پرسمان و مترادف‌ها

```python
QueryExpansion class:
  - load_synonyms()          # بارگذاری مترادف‌ها
  - expand_with_synonyms()   # گسترش
  - set_term_weights()       # وزن‌دهی
  - expand_with_weights()    # وزن‌دهی پرسوجو
  - create_default_synonyms()# مترادف‌های پیش‌فرض
  - save_synonyms()          # ذخیره مترادف‌ها
```

#### 6. `search_system.py` (7.8 KB)

**مسئولیت**: سیستم جستجوی کامل (یکپارچگی تمام اجزاء)

```python
SearchSystem class:
  - initialize()             # تهیه سیستم
  - search()                 # اجرای جستجو
  - display_results()        # نمایش نتایج
  - save_results()           # ذخیره نتایج
  - save_all_models()        # ذخیره تمام مدل‌ها
```

#### 7. `evaluation.py` (6.1 KB)

**مسئولیت**: ارزیابی سیستم و معیار‌های کارایی

```python
SystemEvaluation class:
  - add_query()              # اضافه کردن پرسوجو
  - add_result()             # اضافه کردن نتایج
  - calculate_precision_at_k()
  - evaluate_all_queries()   # ارزیابی
  - print_evaluation_report()
  - calculate_average_precision()
  - save_evaluation_report() # ذخیره گزارش
```

---

### برنامه‌های اجرایی (2 فایل)

#### `main.py` (7.2 KB)

رابط کاربری تعاملی با منوی 7 گزینه‌ای:

```
1. اولین آماده‌سازی سیستم
2. جستجوی معمولی
3. جستجوی با گسترش
4. ارزیابی سیستم
5. مقایسه نتایج
6. ذخیره مدل‌ها
7. خروج
```

#### `demo.py` (2.8 KB)

برنامه نمایشی خودکار که:

- سیستم را تهیه می‌کند
- 3 پرسوجوی نمونه انجام می‌دهد
- ارزیابی را اجرا می‌کند
- تمام مدل‌ها را ذخیره می‌کند

---

### مستندات

#### `README.md` (11.1 KB)

مستندات کامل سیستم شامل:

- معماری کلی
- توضیح تفصیلی هر ماژول
- راهنمای نصب و استفاده
- مثال‌های پرسوجو
- مشخصات فنی

#### `QUICKSTART.md` (7.8 KB)

راهنمای شروع سریع شامل:

- دستورات شروع
- مثال‌های جستجو
- معیار‌های عملکرد
- راهنمای حل مشکلات

#### `PROJECT_SUMMARY.md` (11.6 KB)

خلاصه کامل پروژه شامل:

- 100% بررسی نیازمندی‌ها
- آمار سیستم
- ویژگی‌های پیاده‌شده
- مراحل بعدی

---

### پوشه‌ها

#### `Documents/`

10 سند متنی:

- Doc1.txt (169 توکن) - آب و هوا
- Doc2.txt (168 توکن) - فعالیت‌های روزانه
- Doc3.txt (174 توکن) - برنامه‌ریزی
- Doc4.txt (86 توکن) - فصل‌ها
- Doc5.txt (83 توکن) - ساحل
- Doc6.txt (79 توکن) - کوهستان
- Doc7.txt (72 توکن) - تنوع اقلیمی
- Doc8.txt (201 توکن) - آب و هوا و زیست
- Doc9.txt (192 توکن) - گردشگری
- Doc10.txt (180 توکن) - سفر

#### `output/` (تولید شده)

فایل‌های خروجی:

- `dictionary.txt` - فرهنگ‌لغت
- `inverted_index.txt` - نمایه معکوس
- `compressed_index.txt` - نمایه فشرده
- `tfidf.txt` - مقادیر TF-IDF
- `synonyms.txt` - مترادف‌ها
- `evaluation_demo.txt` - گزارش ارزیابی

---

## 📊 آمار

### کد

```
Module Files:      7 files     ~32 KB
Entry Points:      2 files     ~10 KB
Documentation:     6 files     ~54 KB
Total Python Code: ~1,200 lines
Total Docs:        ~1,500 lines
```

### داده‌ها

```
Documents:         10 files
Total Tokens:      1,404
Unique Terms:      567
Avg Doc Length:    140.4 tokens
```

### معیار‌های کارایی

```
Search Time:       <100ms per query
Processing Time:   ~1-2ms per document
TF-IDF Vectors:    10 documents
Precision@5:       0.20 (20%)
Precision@10:      0.10 (10%)
```

---

## 🚀 شروع سریع

### گزینه 1: حالت تعاملی

```bash
cd d:\Uni\IR\IR_Project\IR_Project
python main.py
```

### گزینه 2: نمایش خودکار

```bash
python demo.py
```

### گزینه 3: استفاده در کد

```python
from search_system import SearchSystem

system = SearchSystem("Documents")
system.initialize()
results = system.search("weather", k=5)
system.display_results(results)
```

---

## ✅ وضعیت

| معیار              | وضعیت             |
| ------------------ | ----------------- |
| پیش‌پردازش         | ✅ تکمیل          |
| فرهنگ‌لغت          | ✅ تکمیل          |
| نمایه معکوس        | ✅ تکمیل          |
| فشردگی‌سازی        | ✅ تکمیل          |
| Vector Space Model | ✅ تکمیل          |
| سیستم جستجو        | ✅ تکمیل          |
| گسترش پرسمان       | ✅ تکمیل          |
| ارزیابی            | ✅ تکمیل          |
| **کلی**            | **✅ 100% تکمیل** |

---

